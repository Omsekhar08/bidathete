import { config } from 'dotenv';
config();

/**
 * Project uses MongoDB (Mongoose). TypeORM/Postgres config removed.
 * Keep a harmless stub so any imports of this file won't crash.
 */

const OrmConfigStub: any = null;
export default OrmConfigStub;
