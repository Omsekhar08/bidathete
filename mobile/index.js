import { AppRegistry } from 'react-native';
import App from './src/App';

// Register the app component with the name "main" — this is what native code expects
AppRegistry.registerComponent('main', () => App);